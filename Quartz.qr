@Encodes<"UTF-8">;

</
    変数宣言
    prep 変数名#型名 >> 数値;
/>

prep number #int >> 12;

prep hoge #string >> "hogehoge";

prep doubleValue >> 3.3; << 型を省略可能(#double)

prep character #char >> "Q";

prep flag #bool >> TRUE; << FALSEとセット

prep nothing #null >> NULL;

</
    定数
    @Final prep#型名 >> 数値;
/>
